import Register from './utils';

export const { init } = Register;
export { default as RegisterProvider } from './components/RegisterProvider';
export { default as useConfig } from './hooks/useConfig';
export { default as useComponent } from './hooks/useComponent';
export * from './types';
